<p id="notice"><%= notice %></p>

<h1>Rockets</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Model</th>
      <th>Astronauts</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @engines.each do |engine| %>
      <tr>
        <td><%= engine.name %></td>
        <td><%= engine.model %></td>
        <td><%= engine.astronauts %></td>
        <td>&nbsp;</td>
        <td><%= link_to 'Show', engine %></td>
        <td>&nbsp;</td>
        <td><%= link_to 'Edit', edit_engine_path(engine) %></td>
        <td>&nbsp;</td>
        <td><%= link_to 'Destroy', engine, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Rocket', new_engine_path %>
